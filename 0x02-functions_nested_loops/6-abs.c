#include "main.h"

/**
 * _abs - func give the absolute value of an int
 *
 * @n: the int checked
 *
 * Return: the absolute value
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

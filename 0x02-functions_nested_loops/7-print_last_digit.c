#include "main.h"

/**
 * print_last_digit - prints the last digit of a number sequence
 *
 * @n: the int returned in the number sequence
 *
 * Return: value of the last digit
 *
 */
int print_last_digit(int n)
{
	int i;

	i = n % 10;

	if (n < 0)
	{
		i = -i;
	}
	return (i)
}
